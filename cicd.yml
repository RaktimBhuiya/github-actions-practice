name: CICD

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        run: echo "Cloning the code"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build the code using Docker
        run: echo "Building Using Docker"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Running Test Cases
        run: echo "Testing the app"
  deploy:
     runs-on: ubuntu-latest
     steps:
       - name: Deploying on the machine
         run: echo "Deploying the first script code "
